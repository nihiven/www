<style>
  body {
    max-width: 1200px;
    margin: 40px auto;
    padding: 0 20px;
    font-family:
      -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu,
      Cantarell, sans-serif;
    color: #c4c4c4;
    background-color: #000000;
  }
  code {
    background: #f4f4f4;
    padding: 2px 6px;
    border-radius: 3px;
    font-family: 'Courier New', monospace;
  }
  pre {
    background: #f4f4f4;
    padding: 15px;
    border-radius: 5px;
    overflow-x: auto;
  }
  pre code {
    background: none;
    padding: 0;
  }
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    margin: 0;
    font-weight: 600;
  }
  a {
    color: #dc66c1;
    text-decoration: none;
  }
  a:hover {
    text-decoration: underline;
  }
  img {
    max-width: 100%;
  }
  blockquote {
    border-left: 4px solid #ddd;
    padding-left: 16px;
    color: #666;
    margin: 16px 0;
  }
  table {
    border-collapse: collapse;
    width: 100%;
    margin: 16px 0;
  }
  table th,
  table td {
    border: 1px solid #ddd;
    padding: 8px 12px;
    text-align: left;
  }
  table th {
    background: #142230;
    font-weight: 600;
  }

  /* Sidebar styling */
  .sidebar {
    display: flex;
    flex-direction: row;
    gap: 8px;
    margin-bottom: 25px;
    justify-content: center;
  }

  .sidebar a {
    padding: 7px 14px;
    background: #1a1a2e;
    border: 1px solid #dc66c1;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 2px;
    font-size: 0.76em;
    transition: all 0.2s;
    white-space: nowrap;
    color: #dc66c1;
  }

  .sidebar a:hover {
    background: #dc66c1;
    color: #000;
  }

  /* Content box styling */
  .content-box {
    border-top: 1px solid #dc66c1;
    border-bottom: 1px solid #dc66c1;
    padding: 10px;
  }

  /* Webring styling */
  .webring-container {
    margin-top: 40px;
    text-align: center;
  }
</style>
